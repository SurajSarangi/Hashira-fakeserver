{
  "name": "fake-server",
  "version": "1.0.0",
  "description": "json server for react app",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "GreyKnight",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.16.3"
  }
}
